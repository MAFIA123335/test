# 🚀 دليل التشغيل والنشر — Beauty Center

> اقرأ ده الأول. الكلام هنا بالمصري وواضح خطوة بخطوة.

---

## ✋ الحقيقة اللي لازم تعرفها

المشروع ده متجر حقيقي بأوردرات ومستخدمين — يعني **لازم قاعدة بيانات**.
مفيش أي متجر في الدنيا بيشتغل من غير داتابيز. عشان كده فيه **خطوة واحدة بس** مستحيل نلغيها:

> **لازم تجيب رابط قاعدة بيانات (DATABASE_URL) وتلزقه في مكان واحد.**

كل حاجة تانية (الجداول، البيانات التجريبية، الأسرار، الأدمن) بتتعمل **لوحدها أوتوماتيك**.

---

## 🗄️ الخطوة 0 — اعمل قاعدة بيانات مجانية (دقيقتين)

عندك اختيارين مجانيين بالكامل:

### الخيار (أ) — Supabase (مُوصى به)
1. روح [supabase.com](https://supabase.com) → اعمل حساب مجاني → **New Project**.
2. استنى لما المشروع يجهز، بعدين:
   **Project Settings → Database → Connection string → URI**.
3. انسخ الرابط. هيبقى شكله كده:
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.xxxx.supabase.co:5432/postgres
   ```
   > بدّل `[YOUR-PASSWORD]` بالباسورد اللي اخترته وقت إنشاء المشروع.

### الخيار (ب) — Neon
1. روح [neon.tech](https://neon.tech) → حساب مجاني → **Create Project**.
2. انسخ الـ **Connection string** من الـ Dashboard.

خد الرابط ده وهتستخدمه في الخطوات الجاية.

---

## 💻 تشغيل محلي على جهازك (Local)

```bash
# 1) الباك اند
cd backend
# افتح ملف .env وبدّل سطر DATABASE_URL بالرابط بتاعك
npm install
npm run dev      # هيعمل الجداول + البيانات التجريبية لوحده أول مرة 🎉

# 2) الفرونت اند (في تيرمينال تاني)
cd frontend
npm install
npm run dev
```

- الموقع: http://localhost:3000
- الـ API: http://localhost:4000/api

**بيانات الدخول الجاهزة:**

| الدور    | الإيميل                     | الباسورد      |
|----------|-----------------------------|---------------|
| أدمن     | admin@beautycenter.com      | Admin@12345   |
| عميل     | customer@beautycenter.com   | Customer@123  |

> ملاحظة: ملف `backend/.env` فيه أسرار (JWT/Cookie) متولّدة جاهزة، فمش محتاج تعمل حاجة غير رابط الداتابيز.

---

## ☁️ النشر أونلاين — 3 خطوات

### 1️⃣ الباك اند على Render (مجاني)
1. ارفع المشروع على GitHub.
2. [render.com](https://render.com) → **New → Blueprint** → اختار الريبو.
   - Render هيقرأ ملف `backend/render.yaml` لوحده.
3. في صفحة الإعدادات هيطلب منك تحط قيمة واحدة:
   - **`DATABASE_URL`** → الزق رابط الداتابيز من الخطوة 0.
   - (وبعد ما تنشر الفرونت، حط `CLIENT_URL` و `CORS_ORIGINS` = رابط موقعك على Vercel).
4. اضغط **Deploy**. أول ما يشتغل هيعمل الجداول + البيانات لوحده.

باقي الأسرار (JWT/Cookie) Render بيولّدها لوحده. مش هتكتب أي مفتاح بإيدك.

### 2️⃣ الفرونت اند على Vercel (مجاني)
1. [vercel.com](https://vercel.com) → **Add New → Project** → اختار نفس الريبو.
2. **Root Directory** = `frontend`.
3. في **Environment Variables** حط:
   - `NEXT_PUBLIC_API_URL` = رابط الـ API من Render + `/api`
     (مثال: `https://beauty-center-api.onrender.com/api`)
   - `NEXT_PUBLIC_SITE_URL` = رابط موقعك على Vercel
4. اضغط **Deploy**.

### 3️⃣ اربط الاتنين
- ارجع لـ Render وحط:
  - `CLIENT_URL` = رابط موقعك على Vercel
  - `CORS_ORIGINS` = نفس الرابط
- اعمل **Redeploy** للباك اند. خلاص 🎉

---

## 🖼️ رفع الصور (اختياري)

المتجر بيشتغل كامل بصور جاهزة من غير أي إعداد. لو عايز الأدمن يرفع صور حقيقية:
1. في Supabase: **Storage → New bucket** باسم `beauty-center` → خليه **Public**.
2. **Project Settings → API** → انسخ:
   - `SUPABASE_URL`
   - `service_role` key → حطه في `SUPABASE_SERVICE_ROLE_KEY`
3. حط القيمتين دول في إعدادات Render (أو ملف `.env` محليًا).

---

## ❓ أسئلة متكررة

**س: ينفع أرفعه على Vercel بس من غير Render؟**
Vercel بترفع الفرونت اند (Next.js). الباك اند سيرفر Express منفصل محتاج استضافة زي Render.
(ممكن مستقبلًا نحوّله serverless على Vercel، بس النسخة دي متقسّمة فرونت/باك عشان تكون أوضح وأسهل صيانة.)

**س: ليه لازم أحط رابط داتابيز؟ مش المفروض يشتغل لوحده؟**
أي متجر بيحفظ أوردرات ومستخدمين لازم داتابيز. الرابط ده الحاجة الوحيدة اللي محدش يقدر يعملها نيابة عنك.
بس بعد ما تحطه — الجداول والبيانات والأدمن كلهم بيتعملوا أوتوماتيك.

**س: البيانات التجريبية اتحطت غلط أو عايز أنضّفها؟**
سيب الداتابيز فاضية والسيرفر هيزرعها أول تشغيل. لو عايز تلغي الزرع التلقائي: حط `AUTO_SEED=false`.

**س: عايز أوقف الـ migration التلقائي؟**
حط `AUTO_MIGRATE=false` وشغّل `npx prisma migrate deploy` بإيدك.
